#define OM_1 ((x) + (x)

#define OM_2 { int x; foo(&x); bar(x);

#define OM_3 (buf[x]1])

#define FM_1(x) ((x) + (x)

#define FM_2(x) { int x; foo(&x); bar(x);

#define FM_3(x) (buf[x]1])

#define VALID_OM ((x) + (x))

#define VALID_FM(x) ((x) + (x))
