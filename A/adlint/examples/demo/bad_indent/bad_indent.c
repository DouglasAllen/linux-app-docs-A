typedef enum { R = 1, G, B } color_t;

color_t c;

int main(int argc, char *argv[])
{
    if (argc < 2)
    exit(1);

for (int i = 0; i < argc; ++i)
puts(argv[i]);

    while (1)
        puts("hello world\n");

    if (c == 3)
        for (int i = 0; i < argc; ++i)
            puts(argv[i]);

    if (argc < 3)
        exit(1);
    else
            do
        puts("hoge\n");
            while (rand());

    if (argc < 4)
        exit(1);
    else if (rand()) {
            puts("hogehoge\n");
    }
    else
        if (rand())
                puts("hogehogehoge\n");

    switch (c) {
    case R:
        puts("kldfaj;\n");
        puts("kldfaj;\n");
    break;
    case G:
    case B:
        puts("kldfaj;\n");
    puts("kldfaj;\n");
    default:
puts("kldfaj;\n");
    }

    return 0;
}
